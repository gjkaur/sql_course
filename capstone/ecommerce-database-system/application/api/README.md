# E-Commerce API Demo

Reuse the mini-backend from Module 5, or extend with:

- GET /products
- GET /products/{id}
- POST /orders (checkout flow with transaction)
- GET /orders (user's orders)

See `module-05-application-integration/mini-backend/` for base implementation.
