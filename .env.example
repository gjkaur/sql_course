# SQL Mastery Course - Environment Variables
# Copy this file to .env and set your values

# PostgreSQL
POSTGRES_USER=postgres
POSTGRES_DB=postgres
DB_PASSWORD=postgres

# DBeaver: Host=localhost Port=5432 Database=postgres User=postgres Password=postgres

# pgAdmin (optional, use --profile tools)
PGADMIN_PASSWORD=sqlcourse
PGADMIN_DEFAULT_EMAIL=admin@postgres.local
